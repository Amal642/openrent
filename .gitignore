logs/
*.jsonl
transcripts/
transcript*/
full-conversations*.md
.env_forhosting
.venv/

# Google service-account credentials must stay outside the repository.
*service-account*.json
gen-lang-client-*.json

# Machine-local session handoff (generated by the PreCompact hook; may contain
# in-flight working context). Keep out of the repo.
docs/handoff-context.md
